{
  "name": "@wms/edge-agent",
  "version": "0.0.1",
  "description": "WMS Edge Agent - Local hardware bridge (DOC-11) + implementação de referência (simulador) usada pelos testes de integração do backend",
  "type": "module",
  "main": "dist/simulator.js",
  "types": "dist/simulator.d.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "socket.io-client": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.0"
  }
}
